@model List<TaskItem>

@{
<<<<<<< HEAD
    ViewData["Title"] = "Tasks List";
}

<H1>@ViewData["Title"]</H1>

<a href="/Tasks/Create" class="btn btn-primary">Add Task</a>


@{
    if(Model != null)
    {
        foreach (var task in Model)
        {
            <p>@task.Title</p>
        }
    }
    else
    {
        <p>Create Tasks</p>
    }

=======
    ViewData["Title"] = "Tasks Management";
}

<div class="container mt-4">
   
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-primary">@ViewData["Title"]</h1>
        <div class="mb-3">
            <!-- Create with pure HTML -->
            <a href="/Tasks/Create" class="btn btn-primary me-2">
                <i class="fas fa-plus"></i> Create (Pure HTML)
            </a>

            <!-- Create with HTML Helper -->
            @Html.ActionLink("Create (HTML Helper)", "CreateWithHelper", "Tasks", null, new { @class = "btn btn-success me-2" })

            <!-- Create with Tag Helper -->
            <a asp-controller="Tasks" asp-action="CreateWithTag" class="btn btn-info">
                <i class="fas fa-plus"></i> Create (Tag Helper)
            </a>
        </div>
    </div>

    @if(Model != null && Model.Any())
    {
        <div class="card shadow-sm">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th scope="col">Title</th>
                            <th scope="col" class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var task in Model)
                        {
                            <tr>
                                <td class="align-middle">@task.Title</td>
                                <td class="text-end">
                                    <div class="btn-group" role="group">
                                        <a href="/Tasks/Details/@task.Id" class="btn btn-info btn-sm" title="Details">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a href="/Tasks/Edit/@task.Id" class="btn btn-warning btn-sm" title="Edit">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a asp-controller="Tasks" asp-action="Delete" asp-route-id="@task.Id"  class="btn btn-danger btn-sm" title="Delete">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <div class="text-center py-5">
            <div class="display-6 text-muted mb-4">
                <i class="fas fa-tasks"></i>
            </div>
            <h3 class="text-muted">No Tasks Found</h3>
            <p class="text-muted">Start by creating your first task</p>
            <a href="/Tasks/Create" class="btn btn-primary mt-3">
                <i class="fas fa-plus"></i> Create First Task
            </a>
        </div>
    }
</div>

@section Styles {
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        .btn-group .btn {
            padding: .375rem .75rem;
            margin: 0 2px;
        }
        .btn-group .btn:hover {
            transform: translateY(-1px);
            transition: transform 0.2s;
        }
        .card {
            border-radius: 10px;
            border: none;
        }
        .table th {
            border-top: none;
            font-weight: 600;
        }
        .table td {
            vertical-align: middle;
        }
    </style>
>>>>>>> 6637ba3848b8a5abbbf0993260116f21b21a1fa6
}
